<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | mJordan Codes</title>
  
  <script src="https://kit.fontawesome.com/a2e16d709b.js" crossorigin="anonymous"></script>

  <link rel="stylesheet" href="/_includes/assets/css/style.css" />
</head>
<body>
  <div id="container">
    <header>
      <span class="lg-title">mJordan Codes</span>
      <nav>
        <ul>
        {% for item in sitemap %}
          <li>
            <a href={{ item.url }}>{{ item.display }}</a>
          </li>
        {% endfor %}
      </ul>
      </nav>
    </header>

    <main>
      <h1>{{ title }}</h1>
      {{ content | safe }}
    </main>

    <aside>
      {% include "components/about/about-aside.njk" %}
    </aside>
  </div>
</body>
</html>